set(CMAKE_PREFIX_PATH "C:/googletest/install")
find_package(GTest REQUIRED)

add_executable(lab_tests
    test_array_sequence.cpp
    test_array.cpp
    test_list.cpp
    test_list_sequence.cpp
    test_bit.cpp
    test_bit_sequence.cpp
    test_algorithms.cpp
    test_main.cpp
)

target_link_libraries(lab_tests PRIVATE GTest::gtest)

include(GoogleTest)
gtest_discover_tests(lab_tests)